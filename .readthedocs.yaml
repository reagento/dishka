version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.14"

python:
  install:
    - method: uv
      path: .
      command: pip # uv pip install -r requirements_doc.txt
      requirements: requirements_doc.txt

sphinx:
  configuration: docs/conf.py
